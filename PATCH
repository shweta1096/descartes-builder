diff --git a/app/external/qtnodes/include/QtNodes/internal/Definitions.hpp b/app/external/qtnodes/include/QtNodes/internal/Definitions.hpp
index 74235e5..1ed92e0 100644
--- a/app/external/qtnodes/include/QtNodes/internal/Definitions.hpp
+++ b/app/external/qtnodes/include/QtNodes/internal/Definitions.hpp
@@ -111,6 +111,20 @@ struct ConnectionId
     PortIndex outPortIndex;
     NodeId inNodeId;
     PortIndex inPortIndex;
+#if 0
+    operator QString() const { 
+        std::ostringstream oss;
+        oss << "(" << outNodeId << ", "
+         << (isPortIndexValid(outPortIndex) ? std::to_string(outPortIndex)
+                                                         : "INVALID")
+         << ", " << inNodeId << ", "
+         << (isPortIndexValid(inPortIndex) ? std::to_string(inPortIndex)
+                                                        : "INVALID")
+         << ")" << std::endl;
+
+        return QString::fromStdString(oss.str());
+    } 
+#endif
 };
 
 inline bool operator==(ConnectionId const &a, ConnectionId const &b)
diff --git a/app/include/ui/models/nodes.hpp b/app/include/ui/models/nodes.hpp
index 6dcf173..d672cca 100644
--- a/app/include/ui/models/nodes.hpp
+++ b/app/include/ui/models/nodes.hpp
@@ -54,6 +54,12 @@ public:
         std::vector<QUuid> inputs;
         std::vector<QUuid> outputs;
         std::pair<size_t, size_t> size() const { return {inputs.size(), outputs.size()}; }
+        void update(unsigned int port, QUuid typeId) {
+            if (port < inputs.size())
+                inputs.at(port) = typeId;
+            else
+                outputs.at(port - inputs.size()) = typeId;  
+        }
         void inverse() { std::swap(inputs, outputs); }
         bool isEmpty() const { return inputs.empty() && outputs.empty(); }
         void print() const { qDebug() << inputs << " => " << outputs; }
diff --git a/app/src/data/custom_graph.cpp b/app/src/data/custom_graph.cpp
index 3ce1248..f1011b5 100644
--- a/app/src/data/custom_graph.cpp
+++ b/app/src/data/custom_graph.cpp
@@ -51,9 +51,13 @@ std::vector<FuncOutModel *> CustomGraph::getFuncOutModels() const
     return result;
 }
 
+// Define a set to track connection IDs for which the message has been printed
+std::unordered_set<QtNodes::ConnectionId> printedConnections;
+
 bool CustomGraph::connectionPossible(QtNodes::ConnectionId const connectionId) const
 {
-    if (auto block = delegateModel<FdfBlockModel>(getNodeId(PortType::In, connectionId)))
+    //std::cerr << "attempt..." << connectionId << std::endl; 
+    if (auto block = delegateModel<FdfBlockModel>(getNodeId(PortType::In, connectionId))) {
         if (auto processorBlock = dynamic_cast<ExternalProcessorModel *>(block)) {
             QUuid outTypeId;
             auto outBlock = delegateModel<FdfBlockModel>(getNodeId(PortType::Out, connectionId));
@@ -63,9 +67,19 @@ bool CustomGraph::connectionPossible(QtNodes::ConnectionId const connectionId) c
             auto result = processorBlock->canConnect(PortType::In,
                                                      getPortIndex(PortType::In, connectionId),
                                                      outTypeId);
-            if (!result)
+            if (!result) {
+                // Check if the message has already been printed for this connection ID
+                if (printedConnections.find(connectionId) == printedConnections.end()) {
+                    qDebug() << "Cannot connect to " 
+                             << connectionId.outNodeId << " " 
+                             << connectionId.outPortIndex;
+                    // Mark this connection ID as printed
+                    printedConnections.insert(connectionId); 
+                }
                 return false;
+            }
         }
+    }
     return DirectedAcyclicGraphModel::connectionPossible(connectionId);
 }
 
diff --git a/app/src/ui/main_window.cpp b/app/src/ui/main_window.cpp
index 2c683b0..ebe0ce3 100644
--- a/app/src/ui/main_window.cpp
+++ b/app/src/ui/main_window.cpp
@@ -56,6 +56,9 @@ MainWindow::MainWindow()
     setGeometry(QApplication::primaryScreen()->availableGeometry());
 
     qInfo() << "Welcome to DesCartes Builder";
+
+    // FIXME
+    m_tabManager->openFrom("/home/eduardoconto/descartes-builder/app/examples/pipe-deformation.dcb");
 }
 
 MainWindow::~MainWindow()
diff --git a/app/src/ui/models/processor_models.cpp b/app/src/ui/models/processor_models.cpp
index 4ea80f7..7e0ba45 100644
--- a/app/src/ui/models/processor_models.cpp
+++ b/app/src/ui/models/processor_models.cpp
@@ -111,6 +111,13 @@ ExternalProcessorModel::ExternalProcessorModel()
 
 bool ExternalProcessorModel::canConnect(PortType portType, PortIndex index, QUuid typeId) const
 {
+    /*
+    qDebug() << "pt:" << portType 
+             << "idx:" << index 
+             << "typeId:" << typeId;
+    
+    m_signature.print();
+    */
     if (m_signature.isEmpty())
         return true;
     if (index == 0)
